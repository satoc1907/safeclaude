# STATUS.md 更新規約（全プロジェクト共通）

ワークスペースのルート（/workspace）に STATUS.md が存在する場合、
セッションで意味のある進捗・決定・ブロッカーが生じたら、作業終了前に必ず更新する。

## frontmatter
- `updated` を現在時刻（ISO 8601, +09:00）に更新する
- `state` は次の3値から「次に動くのは誰か」で選ぶ：
  - `wait_ai` … AIが作業中、またはAIの番（外部待ちもこれに含める）
  - `ready`   … 着手可能。AIにそのまま投げられる状態
  - `review`  … 人（自分）の判断待ち
- `waiting_on` は待ち先：`me`（自分の判断）/ `external`（査読者・事務局など外部）/ `none`
- **`waiting_on: external` のとき `state` を `ready` にしない**（外部待ちは着手可能ではない）
- `phase` は idea / dev / writing / review / revision / done / admin から選ぶ

## 本文
- 「現状サマリ」は3行以内。経緯の要約ではなく「今の状態」を書く
- 「進行中」「ブロッカー・要判断」のタスク行には `@ai` / `@me` / `@ext` を付ける
- 完了したタスクは「完了」セクションへ移す
- 「次にやるべきこと（AI提案）」は、次セッションの人間が最初に読む前提で更新する

## 重要な制約
- STATUS.md が存在しないリポジトリでは、明示的に頼まれない限り新規作成しない
  （STATUS.md を置く = ダッシュボードの追跡対象にする、という opt-in 運用のため）
- .docx などバイナリ中心のリポでは、中身から判断できない事項（誰の返事待ちか、
  締切、優先度など）を推測で埋めず、「人間が確認すべき項目」として報告する
- 依頼された作業の対象外のファイルは変更しない

@RTK.md
